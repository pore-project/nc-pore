name: V1 Talk Connector

on:
  push:
    branches:
      - fix-v1-final
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke
        run: echo "V1 workflow smoke test OK"
