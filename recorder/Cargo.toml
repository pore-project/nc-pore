[package]
name = "recorder"
version = "0.1.0"
edition = "2024"

[dependencies]
cpal = "0.18.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
criterion = "0.7"

[[bench]]
name = "payload_hash"
harness = false
