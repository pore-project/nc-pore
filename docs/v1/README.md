# PoRE V1

This directory contains implementation notes for the first usable Talk integration milestone.

The V1 recording lifecycle is explicitly controlled by the host: recording starts and stops independently of the Talk room lifecycle. Room termination is cleanup, not the normal recording-stop signal.
